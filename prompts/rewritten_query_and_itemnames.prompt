历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题完成两件事：
1. 提取用户正在询问的【金融主体】（item_names）：可能是金融产品名称、机构名称或资料名称；
   如用户使用代词（"这个""它"），结合历史做指代消解；
   如用户问的是通用金融概念（如"什么是净值型理财"）或业务流程规则（如"赎回多久到账"），
   无法确定具体主体时，item_names 返回空列表。
2. 改写用户问题（rewritten_query），使其成为独立、完整、含上下文的问题。

请直接返回 JSON，格式如下：
{{
    "item_names": ["产品A", "机构B"],
    "rewritten_query": "关于产品A和机构B，……"
}}